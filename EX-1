import pandas as pd
import matplot.pyplot as plt
import seaborn as sns
from scipy.ststs import skew, kurtosis
iris = sns,load_dataset('iris')
attribute = "speal_length"
print(iris.head())
sns.histplot(iris[attributr], kde=True)
plt.title(f"Histogram and KDE of {attribute}")
plt.show()
mean=iris[attribute].mean()
std=iris[attribute].std()
x = range(int(iris[attribute].min()),int(iris[attribute].max()0+1)
y = [1/(std * (2*np.pi)**0.5) * np.exp(-0.5*((i-mean)/std)**2) for i in x]
plt.plot(x,y)
plt.title(f"Probability Distribution of {attribute}")
print(f"Skewness of {attribute}:", skew(iris[attribute]))
print(f"Kurtosis of {attribute}:", kurtisis(iris[attribute]))
